<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChroZenService.View_Error">
  <ContentView.Content>
        <AbsoluteLayout>
            <Grid >
                <Image Source="monitor_error.jpg" Aspect="Fill" />
                <Button Margin="205,865,210,0" BackgroundColor="Transparent" Command="{Binding ErrorCloseSelect}"/>
                <Button Margin="405,865,10,0" BackgroundColor="Transparent" Command="{Binding ErrorResetSelect}"/>
            </Grid>
        </AbsoluteLayout>
  </ContentView.Content>
</ContentView>